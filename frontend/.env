# Do not put localhost URLs here.
# Vite loads `.env` for both dev and production builds (including Vercel).
#
# Local dev: use `.env.development` (committed) or `.env.local` (not committed).
# Production (Vercel): set env vars in the Vercel dashboard.

VITE_BACKEND_URL=https://edu-nest-backend-tau.vercel.app
